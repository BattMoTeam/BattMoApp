{
    "$id": "file://./cell_type.schema.json",
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "description": "Cell type object",
    "type": "object",
    "required": ["display_name", "is_shown_to_user", "context_type", "context_type_iri"],
    "properties": {
        "display_name": {
            "type": "string",
            "description": "The name that is rendered in the app"
        },
        "is_shown_to_user":{
            "type": "bool",
            "description": "Whether the cell type is rendered in the app or not.",
            "enum": [true, false]
        },
        "context_type":{
            "type": ["string", "list"],
            "description": "The ontology context type of the object."
        },
        "context_type_iri": {
            "type": "string",
            "description": "The iri of the ontology context type."
        }
    }
}