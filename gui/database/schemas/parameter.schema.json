{
    "$id": "file://./parameter.schema.json",
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "description": "Parameter object",
    "type": "object",
    "required": ["display_name", "is_shown_to_user", "context_type", "context_type_iri", "type"],
    "properties": {
        "display_name": {
            "type": "string",
            "description": "The name that is rendered in the app"
        },
        "is_shown_to_user":{
            "type": "bool",
            "description": "Whether the material is rendered in the app or not.",
            "enum": [true, false]
        },
        "context_type":{
            "type": ["string", "list"],
            "description": "The ontology context type of the object."
        },
        "context_type_iri": {
            "type": "string",
            "description": "The iri of the ontology context type."
        },
        "type":{
            "type": "string",
            "description": "The type of the parameter value.",
            "enum": ["float", "int", "bool", "string"]
        },
        "min_value": {
            "type": "number",
            "description": "The minimum value that the parameter can be."
        },
        "max_value": {
            "type": "number",
            "description": "The maximum value that the parameter can be."
        },
        "unit": {
            "type": "string",
            "description": "The unit of the parameter in symbol format."
        },
        "unit_name": {
            "type": "string",
            "description": "The unit context type of the parameter."
        },
        "unit_iri": {
            "type": "string",
            "description": "The unit context type iri of the paramter in symbol format."
        }

    }
}